#!/usr/bin/env sh

# Date command
# ============
# Tim Friske <me@tifr.de>
#
# Shows local date.
#
# Prints `date-ldt: DATE-LDT` if standard output file descriptor is
# connected to terminal, else just `DATE-LDT`.
#
# Deps:: date, env, sh

set -o errexit -o nounset

[ -t 1 ] && printf 'date-ldt: '
date "${@}"
