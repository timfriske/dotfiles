#!/usr/bin/env sh

# Even command
# ============
# Tim Friske <me@tifr.de>
#
# Prints lines on even line numbers.
#
# Deps:: env, sh, awk

set -o errexit -o nounset

main() {
  awk 'FNR%2==0' "${@}"
}

main "${@}"
