#!/usr/bin/env sh

# Fix Last End-Of-Line command
# ============================
# Tim Friske <me@tifr.de>
#
# Adds an UNIX end-of-line character (\n) to the last line of each given
# text file if missing. This is the format expected by the overwhelming
# majority of UNIX/Linux tools.
#
# Deps:: env, sed, sh

set -o errexit -o nounset

sed --expression='$a\' --in-place "${@}"
