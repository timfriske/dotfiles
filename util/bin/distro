#!/usr/bin/env sh

# Distro command
# ==============
# Tim Friske <me@tifr.de>
#
# Dependencies:
#   sh:: POSIX-compatible shell.
#   uname:: Uname command.

set -o errexit -o nounset

main() {
  lsb_release --all
}

main "$@"
