#!/usr/bin/env bash

# Distro command
# ==============
# Tim Friske <me@tifr.de>
#
# Deps:: env, lsb_release, bash

# Ensure fail-fast script execution.
shopt -s inherit_errexit
shopt -so nounset pipefail errtrace errexit

lsb_release --all
