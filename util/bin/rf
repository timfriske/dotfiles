#!/usr/bin/env sh

# Remove command
# ==============
# Tim Friske <me@tifr.de>
#
# Remove files, ignore nonexistent files and arguments, never prompt.
# Do not remove the root directory and everything below it. Explain
# what is being done.
#
# Deps:: env, rm, sh

set -o errexit -o nounset

rm --preserve-root --force --verbose "${@}"
