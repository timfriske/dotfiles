# Bash aliases for Mkdir
# ======================
# Tim Friske <me@tifr.de>

# Call `mkdir` with default options.
#
alias mkdir='mkdir $MKDIR_X_OPTIONS'

# Alias `md` resembles `cp`, `ln`, `mv` etc.
#
alias md='mkdir'
