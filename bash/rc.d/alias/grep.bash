# Bash aliases for Grep
# =====================
# Tim Friske <me@tifr.de>

# Call `grep` with default options.
#
alias grep='grep $GREP_X_OPTIONS'

alias egrep='grep --extended-regexp'
alias fgrep='grep --fixed-strings'
