#!/usr/bin/env sh

# Git Fetch command
# =================
# Tim Friske <me@tifr.de>
#
# Dependencies:
#   env:: Env command.
#   git-fetch:: Git Fetch command.
#   sh:: POSIX-compatible shell.

set -o errexit -o nounset

main() {
  git fetch --recurse-submodules=on-demand --prune --prune-tags \
    --jobs=2 "$@"
}

main "$@"
