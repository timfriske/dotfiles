#!/usr/bin/sh

# Git Fetch command
# =================
# Tim Friske <me@tifr.de>
#
# Dependencies:
#   git-fetch:: Git Fetch command.
#   sh:: POSIX-compatible shell.

set -o errexit -o nounset

main() {
  git fetch --recurse-submodules=on-demand --prune --jobs=2 "$@"
}

main "$@"
