#!/usr/bin/env sh

# Git Rebase command
# ==================
# Tim Friske <me@tifr.de>
#
# Deps:: env, git-rebase, sh

set -o errexit -o nounset

git rebase --interactive ${GIT_X_SIGNOFF:-} --onto "${@}"
