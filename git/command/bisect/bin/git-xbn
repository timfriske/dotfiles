#!/usr/bin/env sh

# Git Bisect command
# ==================
# Tim Friske <me@tifr.de>
#
# Dependencies:
#   env:: Env command.
#   git-bisect:: Git Bisect command.
#   sh:: POSIX-compatible shell.

set -o errexit -o nounset

main() {
  git bisect new "${@}"
}

main "${@}"
