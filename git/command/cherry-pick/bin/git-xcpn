#!/usr/bin/env sh

# Git Cherry-pick command
# =======================
# Tim Friske <me@tifr.de>
#
# Dependencies:
#   env:: Env command.
#   git-cherry-pick:: Git Cherry-pick command.
#   sh:: POSIX-compatible shell.

set -o errexit -o nounset

main() {
  git cherry-pick --no-commit $GIT_X_SIGNOFF "${@}"
}

main "${@}"
