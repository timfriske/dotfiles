#!/usr/bin/env bash

# Git Reset command
# =================
# Tim Friske <me@tifr.de>
#
# Deps:: bash, env, git-reset

# Ensure fail-fast script execution.
shopt -os nounset noclobber pipefail errexit errtrace

git reset --mixed -N "${@}"
