#!/usr/bin/env sh

# Git Reset command
# =================
# Tim Friske <me@tifr.de>
#
# Dependencies:
#   env:: Env command.
#   git-reset:: Git Reset command.
#   sh:: POSIX-compatible shell.

set -o errexit -o nounset

main() {
  git reset --patch "${@}"
}

main "${@}"
