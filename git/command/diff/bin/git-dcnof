#!/usr/bin/env bash

# Git Diff command
# ================
# Tim Friske <me@tifr.de>
#
# Deps:: bash, env, git-diff

# Ensure fail-fast script execution.
shopt -os nounset pipefail errexit errtrace

cd "${GIT_PREFIX:-.}"
git diff --cached --name-only --diff-filter="${1:-ACDMRTUXB}" "${@:2}"
