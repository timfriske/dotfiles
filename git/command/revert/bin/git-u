#!/usr/bin/env sh

# Git Revert command
# =================
# Tim Friske <me@tifr.de>
#
# Dependencies:
#   env:: Env command.
#   git-revert:: Git Revert command.
#   sh:: POSIX-compatible shell.

set -o errexit -o nounset

main() {
  git revert "${@}"
}

main "${@}"
