#!/usr/bin/env bash

# Kubectl command
# ===============
# Tim Friske <me@tifr.de>
#
# Deps:: bash, env, kubectl, watch

shopt -os nounset pipefail errexit errtrace

if [[ -t 1 ]]; then
  watch --differences --errexit kubectl top node "${@}"
else
  kubectl top node "${@}"
fi
