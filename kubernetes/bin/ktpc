#!/usr/bin/env bash

# Kubectl command
# ===============
# Tim Friske <me@tifr.de>
#
# Deps:: bash, env, kubectl, watch

# Ensure fail-fast script execution.
shopt -os nounset pipefail errexit errtrace

if [[ -t 1 ]]; then
  watch --differences kubectl top pod --sort-by=cpu "${@}"
else
  kubectl top pod --sort-by=cpu "${@}"
fi
