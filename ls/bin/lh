#!/usr/bin/env sh

# Ls command
# ==========
# Tim Friske <me@tifr.de>
#
# Dependencies:
#   env:: Env command.
#   ls:: Ls command.
#   sh:: POSIX-compatible shell.

set -o errexit -o nounset

main() {
  ls ${LS_X_OPTIONS:-} --almost-all --ignore="*" "$@"
}

main "$@"
